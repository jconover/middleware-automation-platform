[defaults]
# Roles path relative to project root (for AWX compatibility)
roles_path = automated/ansible/roles
inventory = automated/ansible/inventory/dev.yml
host_key_checking = False
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
