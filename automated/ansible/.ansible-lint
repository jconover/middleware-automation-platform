---
# Ansible-lint configuration for Enterprise Middleware Automation Platform
# Documentation: https://ansible.readthedocs.io/projects/lint/configuring/

profile: production

# Offline mode for CI/CD environments
offline: false

# Paths to exclude from linting
exclude_paths:
  - .cache/
  - .git/
  - molecule/
  - collections/

# Skip specific rules with justification
skip_list:
  - role-name  # Using descriptive names (liberty, monitoring) not galaxy format

# Rules to warn about but not fail
warn_list:
  - experimental
  - yaml[line-length]  # Some templates have long lines

# Enable strict mode for production-quality code
strict: true

# Use default rules
use_default_rules: true

# Limit output
parseable: true
quiet: false

# Mock modules for linting without installing collections
mock_modules:
  - amazon.aws.ec2_instance
  - amazon.aws.aws_ec2

# Mock roles
mock_roles:
  - common
  - liberty
  - monitoring
  - postgresql
  - redis
  - nginx

# Variable naming convention
var_naming_pattern: "^[a-z_][a-z0-9_]*$"

# Task naming - require descriptive names
task_name_prefix: "{stem} | "

# Loop variable naming
loop_var_prefix: "^(item|idx|key|value|host|file|pkg|svc|user|port|dir|path)"
