# =============================================================================
# Ansible Inventory for Jenkins AWS Deployment
# =============================================================================
# Copy this file to inventory.ini and update the values
#
# To get the management server IP:
#   cd automated/terraform/environments/prod-aws
#   terraform output management_public_ip
# =============================================================================

[management]
mw-prod-management ansible_host=<MANAGEMENT_PUBLIC_IP> ansible_user=ec2-user

[management:vars]
# SSH key for EC2 access (update path to your key)
ansible_ssh_private_key_file=~/.ssh/mw-prod-deployer.pem

# Python interpreter
ansible_python_interpreter=/usr/bin/python3

# SSH options
ansible_ssh_common_args='-o StrictHostKeyChecking=no'
